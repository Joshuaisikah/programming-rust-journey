# Chapter 12 Project: Vector Math Library

Overload operators for 2D/3D vectors.

```rust
let v1 = Vec2::new(1.0, 2.0);
let v2 = Vec2::new(3.0, 4.0);
let sum = v1 + v2;  // Operator overloading!
```

Master operator overloading! ➕
