use json_db::Database;

fn main() {
    println!("json-db — Capstone 2");
    println!("Run `cargo test` to see which tests pass.");
    println!();
    println!("Implement the modules in src/ to make the tests pass:");
    println!("  src/document.rs   — Document model");
    println!("  src/collection.rs — CRUD operations");
    println!("  src/query.rs      — Query DSL");
    println!("  src/index.rs      — Field indexes");
    println!("  src/storage.rs    — Disk persistence");
    println!("  src/transaction.rs— WAL transactions");
}
